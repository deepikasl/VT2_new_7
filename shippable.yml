language: node_js

# Version number
node_js:
  - 0.12
  - 0.11

build:
  ci:
    - node --version
    - source '$(shipctl get_resource_state VT1_runCI/VT1_runCI.env)'
    - echo "TRIGGER_REASON-->"$TRIGGER_REASON
    
integrations:
  notifications:  
    - integrationName: email
      type: email
      recipients: 
        - deepika@shippable.com
      sendConsoleLogs: true
      sendCoverageReports: true
      sendTestReports: true
      

